<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ready2eat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        tools:replace="android:icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".View.AboutUs"></activity>
        <activity android:name=".View.OrderDetail"></activity>

        <service
            android:name=".Service.ListenOrder"
            android:enabled="true"
            android:exported="true" />
        <activity android:name=".View.Admin.AdminFoodDetail" />
        <activity android:name=".View.Admin.AdminOrderStatus" />
        <activity android:name=".View.Admin.AdminFoodList" />
        <activity
            android:name=".View.Admin.AdminHome"
            android:label="@string/title_activity_admin_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.OrderStatus" />
        <activity android:name=".View.Cart" />
        <activity android:name=".View.FoodDetail" />
        <activity android:name=".View.FoodList" />
        <activity
            android:name=".View.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.SignUp" />
        <activity android:name=".View.LogIn" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>